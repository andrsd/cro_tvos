<document>
  <compilationTemplate>
    <list>
      <relatedContent>
        <itemBanner>
          {{#if show.attributes.asset.url}}
          <heroImg src="{{ show.attributes.asset.url }}" />
          {{/if}}
          <description id="show-description" style="tv-text-max-lines: 2">{{removeHTML show.attributes.description }}</description>
          <row>
            <buttonLockup id="fav-btn">{{{ ratedButton }}}</buttonLockup>
          </row>
        </itemBanner>
      </relatedContent>
      <header>
        <title>{{ show.attributes.title }}</title>
        <row>
          <text>{{ show.relationships.episodes.meta.count }} epizod</text>
          {{#if show.attributes.active}}
          <text>Aktivní</text>
          {{/if}}
          {{#if show.attributes.podcast}}
          <text>Podcast</text>
          {{/if}}
        </row>
      </header>
      <section>
        {{#each episodes}}
        <listItemLockup data-href-page="now-playing" data-href-page-options="{{toJSON this }}">
          <ordinal minLength="2">{{{watchedState watched }}}</ordinal>
          <title>{{ attributes.title }}</title>
          <subtitle>{{longDate attributes.since }}  {{timeLength attributes.since attributes.till }}  {{#if attributes.part}}({{attributes.part}}. díl){{/if}}</subtitle>
        </listItemLockup>
        {{/each}}
        {{#if links.next}}
        <listItemLockup data-href-page="show" data-href-page-options="{{toJSON @root.links }}">
          <title>Další...</title>
        </listItemLockup>
        {{/if}}
      </section>
      {{#if serials}}
      <section>
        <header>
          <title></title>
        </header>
        {{#each serials }}
        <listItemLockup data-href-page="serial" data-href-page-options="{{toJSON this }}">
          {{#if attributes.asset.url}}
          <img src="{{ attributes.asset.url }}" class="show-icon" contentsMode="aspectFill"/>
          {{else}}
          <img src="" class="show-icon" contentsMode="aspectFill"/>
          {{/if}}
          <title>{{ attributes.title }}</title>
          <subtitle>Seriál {{#if attributes.totalParts}}{{numParts attributes.totalParts}}{{/if}}</subtitle>
        </listItemLockup>
        {{/each}}
      </section>
      {{/if}}
    </list>
  </compilationTemplate>
</document>
